import 'package:flutter/material.dart';

/// Paleta de colores de la aplicación
class AppColors {
  AppColors._(); // Constructor privado para evitar instancias

  /// Color principal: azul ThingsBoard
  static const Color primary = Color.fromRGBO(8, 100, 167, 1);

  // aquí podrías agregar más:
  // static const Color accent = Color(0xFFCRC...);
  // static const Color background = ...
}
